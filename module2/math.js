function degToRad(degrees) {
    return degrees * (Math.PI / 180);
  }
  
  function radToDeg(rad) {
    return rad / (Math.PI / 180);
  }

  console.log("Grados a radianes ",degToRad(60));
  console.log("Radianes a Grados ",radToDeg(1.04));

